apiVersion: v1
kind: Service
metadata:
  name: notflux-mcp-server
  namespace: ping-devops-cprice
spec:
  selector:
    app: notflux-mcp-server
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
