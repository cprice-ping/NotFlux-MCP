google-cloud-aiplatform[adk,reasoningengine]>=1.87.0
# [mcp] extra installs the `mcp` package that agent.py's McpToolset imports.
# Plain `google-adk` does NOT include it — omitting the extra makes the deployed
# Agent Engine container crash on startup with ModuleNotFoundError: No module named 'mcp'.
google-adk[mcp]>=1.5.0
requests>=2.32.0
